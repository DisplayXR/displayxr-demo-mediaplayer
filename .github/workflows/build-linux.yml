name: Build Linux

# mediaplayer Linux build-green dev loop (issue #30; recipe: the runtime repo's
# docs/guides/linux-demo-port.md).
#
# TRI-LTS MATRIX (mirrors displayxr-demo-modelviewer): the deployment target is
# Ubuntu 26.04 + Intel iGPU, but we also keep 22.04 (where render can be
# validated on hand today) and 24.04 green — so "build-green" means "builds on
# every supported toolchain (GCC 11 / 13 / 15, ffmpeg 4.4 / 6 / 7)", not just
# whatever ubuntu-latest happens to be. 22.04/24.04 use hosted runners; 26.04
# runs in a container (no hosted 26.04 runner yet).
#
# PR-triggered required check: runs on every PR (plus manual dispatch and
# `linux*` branches), with the DetectChanges docs_only short-circuit the
# macOS/Windows jobs use so doc-only PRs report success in seconds without
# building. Required contexts (main-protection ruleset): Build (Ubuntu 22.04 /
# 24.04 / 26.04).
#
# Packaging gate: the "Linux Packaging" aggregator job (below) is the single
# required context for the Deb + DebInstall (Ubuntu 22.04 / 24.04 / 26.04)
# lane, so a .deb that installs but cannot run on a supported release blocks
# the merge, not just the release (runtime #1656 / #1684).
#
# Build-green scope: compile mediaplayer_handle_vk_linux. CI has no
# display/GPU, so the app is never run here; on-screen validation is
# a separate pass gated on the runtime's Linux Phase 1b + hardware.

on:
  workflow_dispatch: {}
  pull_request: {}
  push:
    branches:
      - 'linux'
      - 'linux-**'
      - 'feature/linux-**'
    # Release tags: the Deb job attaches the demo .deb to the GitHub Release
    # (the Linux meta-bundle fetches it — runtime #781 Phase 3).
    tags:
      - 'v*'

# The Deb job attaches release assets on tags.
permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  DetectChanges:
    runs-on: ubuntu-latest
    outputs:
      docs_only: ${{ steps.filter.outputs.docs_only }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - id: filter
        run: |
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            echo "docs_only=false" >> "$GITHUB_OUTPUT"
            echo "Detected non-PR trigger — docs_only=false"
            exit 0
          fi
          CHANGED=$(git diff --name-only "${{ github.event.pull_request.base.sha }}" HEAD)
          NON_DOC=$(echo "$CHANGED" | grep -vE '^(docs/|.*\.md$|LICENSE$|\.github/ISSUE_TEMPLATE/)' || true)
          if [ -z "$NON_DOC" ]; then
            echo "docs_only=true" >> "$GITHUB_OUTPUT"
          else
            echo "docs_only=false" >> "$GITHUB_OUTPUT"
          fi
          echo "Detected docs_only=$(grep ^docs_only= "$GITHUB_OUTPUT" | cut -d= -f2)"

  Build:
    name: Build (Ubuntu ${{ matrix.lts }})
    needs: DetectChanges
    runs-on: ${{ matrix.runner }}
    container: ${{ matrix.container }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - lts: "22.04"
            runner: ubuntu-22.04
          - lts: "24.04"
            runner: ubuntu-24.04
          - lts: "26.04"
            runner: ubuntu-latest
            container: ubuntu:26.04
    steps:
      - name: Docs-only short-circuit
        if: needs.DetectChanges.outputs.docs_only == 'true'
        run: echo "Doc-only change — reporting success without building."

      # In the 26.04 container we start as root with a bare image — install git
      # BEFORE checkout (which shells out to git). No-op on the hosted runners.
      - name: Bootstrap container (git for checkout)
        if: needs.DetectChanges.outputs.docs_only != 'true'
        run: |
          if [ "$(id -u)" = "0" ] && ! command -v git >/dev/null; then
            apt-get update && apt-get install -y --no-install-recommends git ca-certificates
          fi

      - uses: actions/checkout@v5
        if: needs.DetectChanges.outputs.docs_only != 'true'

      # Beyond the playbook's base list: FFmpeg dev packages (else the whole
      # media path — including the Linux VAAPI branch — silently compiles out
      # behind MEDIAPLAYER_WITH_FFMPEG), and the X11/Wayland + ALSA headers
      # SDL3 needs to build meaningful video/audio backends from source (this
      # demo is a _handle app that opens its own SDL window). libxxf86vm-dev +
      # libxcb-glx0-dev: once GL/EGL headers are present, OpenXR-SDK-Source
      # flips on GLX platform support — the loader includes <xcb/glx.h> and
      # its openxr-gfxwrapper test helper links Xxf86vm.
      # SUDO shim so the same step works on hosted runners (non-root, has sudo)
      # and in the container (root, no sudo). Vendor-neutral libegl-dev/libgl-dev
      # so the names resolve across 22.04/24.04/26.04.
      - name: Install dependencies
        if: needs.DetectChanges.outputs.docs_only != 'true'
        run: |
          SUDO=""; [ "$(id -u)" != "0" ] && SUDO=sudo
          $SUDO apt-get update
          $SUDO apt-get install -y \
            build-essential cmake ninja-build pkg-config git ca-certificates curl \
            libvulkan-dev vulkan-validationlayers glslang-tools \
            libavformat-dev libavcodec-dev libavutil-dev \
            libswscale-dev libswresample-dev \
            libx11-dev libxext-dev libxcursor-dev libxi-dev libxrandr-dev \
            libwayland-dev libxkbcommon-dev libegl-dev libgl-dev libasound2-dev \
            libxxf86vm-dev libxcb-glx0-dev

      - name: Build (build-green; app is not run — no display on CI)
        if: needs.DetectChanges.outputs.docs_only != 'true'
        run: ./scripts/build_linux.sh

      # Guard against a hollow green: FFmpeg must have resolved via pkg-config
      # so the media/VAAPI path actually compiled.
      - name: Assert FFmpeg media path compiled
        if: needs.DetectChanges.outputs.docs_only != 'true'
        run: |
          grep -q "pkgcfg_lib_FFMPEG_avcodec" build/CMakeCache.txt || {
            echo "::error::FFmpeg was not found via pkg-config — media path compiled out"; exit 1; }

  # Build the ONE demo .deb and, on a v* tag, attach it to the GitHub Release —
  # the asset the Linux meta-bundle fetches (runtime #781 Phase 3; the bundle's
  # glob `displayxr-mediaplayer_*_amd64.deb` expects exactly one). Runs on every
  # PR too so a broken package_deb_linux.sh is caught before release.
  #
  # PORTABLE BY CONSTRUCTION (#76): the same .deb must install and run on Ubuntu
  # 22.04, 24.04 and 26.04, so it is built on the OLDEST (ubuntu:22.04 container:
  # glibc 2.35 / GCC 11 floor), links SDL3 statically (24.04 has no libsdl3), and
  # bundles a private slim FFmpeg (distro sonames are libavcodec58/60/62). The
  # DebInstall job below proves it on all three.
  Deb:
    needs: DetectChanges
    runs-on: ubuntu-latest
    container: ubuntu:22.04
    env:
      FFMPEG_PREFIX: /tmp/ffmpeg-slim-linux
      CMAKE_VERSION: "3.31.8"   # 22.04's apt cmake (3.22) is below CMakeLists' 3.24 floor
    steps:
      - name: Docs-only short-circuit
        if: needs.DetectChanges.outputs.docs_only == 'true'
        run: echo "Doc-only change — skipping .deb build."

      # Bare image, root: git before checkout (else checkout falls back to a
      # tarball with no tags and git describe cannot version the .deb).
      - name: Bootstrap container (git for checkout)
        if: needs.DetectChanges.outputs.docs_only != 'true'
        run: |
          apt-get update
          apt-get install -y --no-install-recommends git ca-certificates curl

      - uses: actions/checkout@v5
        if: needs.DetectChanges.outputs.docs_only != 'true'
        with:
          fetch-depth: 0 # git describe needs tags for the .deb version

      # No system FFmpeg or SDL3 dev packages ON PURPOSE: neither may reach the
      # .deb. nasm + libva-dev + zlib1g-dev build the private FFmpeg; the X11 /
      # Wayland / PulseAudio / PipeWire / ALSA headers let the static SDL3
      # compile those backends as runtime dlopen()s (package_deb_linux.sh fails
      # if one is missing). python3 + libxxf86vm-dev + libxcb-glx0-dev: the
      # from-source OpenXR loader (codegen; GLX platform support once GL headers
      # are present).
      - name: Install dependencies
        if: needs.DetectChanges.outputs.docs_only != 'true'
        run: |
          apt-get install -y --no-install-recommends \
            build-essential ninja-build pkg-config python3 file binutils \
            libvulkan-dev glslang-tools \
            nasm libva-dev zlib1g-dev \
            libx11-dev libxext-dev libxcursor-dev libxi-dev libxrandr-dev libxfixes-dev \
            libwayland-dev wayland-protocols libxkbcommon-dev libegl-dev libgl-dev \
            libasound2-dev libpulse-dev libpipewire-0.3-dev libdbus-1-dev libudev-dev \
            libxxf86vm-dev libxcb-glx0-dev \
            dpkg-dev fakeroot
          curl -fsSL "https://github.com/Kitware/CMake/releases/download/v${CMAKE_VERSION}/cmake-${CMAKE_VERSION}-linux-x86_64.tar.gz" \
            | tar -xz -C /opt
          echo "/opt/cmake-${CMAKE_VERSION}-linux-x86_64/bin" >> "$GITHUB_PATH"

      - name: Cache slim FFmpeg
        if: needs.DetectChanges.outputs.docs_only != 'true'
        uses: actions/cache@v4
        with:
          path: ${{ env.FFMPEG_PREFIX }}
          key: ffmpeg-slim-linux-2204-${{ hashFiles('scripts/build_ffmpeg_linux.sh') }}

      - name: Build + package the demo .deb
        if: needs.DetectChanges.outputs.docs_only != 'true'
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          cmake --version | head -1
          ./scripts/package_deb_linux.sh
          ls -lh dist/*.deb

      - name: Upload .deb artifact
        if: needs.DetectChanges.outputs.docs_only != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: displayxr-mediaplayer-deb
          path: dist/*.deb
          if-no-files-found: error

      - name: Attach .deb to the GitHub Release (tags only)
        if: needs.DetectChanges.outputs.docs_only != 'true' && startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.deb
          fail_on_unmatched_files: true

  # Install the .deb from the Deb job into a PRISTINE container of every
  # supported release, with the latest released runtime .deb (Depends:
  # displayxr-runtime). Fails if apt cannot resolve Depends there, if `ldd -r`
  # shows any unresolved library / symbol / symbol version, if a system SDL3 or
  # FFmpeg is picked up, or if a Recommends/Suggests name is not in that archive.
  DebInstall:
    name: DebInstall (Ubuntu ${{ matrix.lts }})
    needs: [DetectChanges, Deb]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        lts: ["22.04", "24.04", "26.04"]
    steps:
      - name: Docs-only short-circuit
        if: needs.DetectChanges.outputs.docs_only == 'true'
        run: echo "Doc-only change — no .deb to verify."

      - uses: actions/checkout@v5
        if: needs.DetectChanges.outputs.docs_only != 'true'

      - uses: actions/download-artifact@v4
        if: needs.DetectChanges.outputs.docs_only != 'true'
        with:
          name: displayxr-mediaplayer-deb
          path: dist

      - name: Fetch the latest released runtime .deb
        if: needs.DetectChanges.outputs.docs_only != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release download --repo DisplayXR/displayxr-runtime \
            --pattern 'displayxr-runtime_*_amd64.deb' --dir runtime-deb
          ls runtime-deb

      - name: apt install + ldd in clean ubuntu:${{ matrix.lts }}
        if: needs.DetectChanges.outputs.docs_only != 'true'
        run: |
          docker run --rm -v "$PWD:/w" -w /w "ubuntu:${{ matrix.lts }}" \
            ./scripts/verify_deb_install_linux.sh dist/displayxr-mediaplayer_*_amd64.deb runtime-deb/*.deb

  # Single stable required context for the whole Linux packaging lane —
  # "Linux Packaging" (mirrors displayxr-runtime's, PR #1684).
  #
  # Why an aggregator rather than requiring the legs directly: DebInstall is a
  # matrix, so its contexts are "DebInstall (Ubuntu 22.04)", "… 24.04",
  # "… 26.04" — names that change the moment a release is added or dropped, and
  # a ruleset still demanding a context no job produces blocks every PR forever.
  # This job's name is fixed, so the matrix can grow or shrink freely.
  #
  # The gap it closes: DebInstall proves the .deb installs AND runs on every
  # supported release, but nothing stopped a PR MERGING with it red — the
  # runtime #1656 shape (a .deb that apt installs on 22.04 and that then fails
  # at exec / dlopen) was only caught at release time, if at all.
  #
  # Doc-only PRs: Deb / DebInstall still RUN on the docs_only path and skip
  # every step, so this job's needs are satisfied and it reports success in
  # seconds without asserting anything.
  #
  # `if: always()` is load-bearing: without it a failed leg would SKIP this job,
  # and a skipped check counts as green for a required context — the aggregator
  # would silently stop gating exactly when it matters.
  LinuxPackaging:
    name: Linux Packaging
    needs: [DetectChanges, Deb, DebInstall]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Docs-only short-circuit
        if: needs.DetectChanges.outputs.docs_only == 'true'
        run: echo "Doc-only change — packaging legs short-circuited; reporting success without asserting."

      - name: Assert every packaging leg succeeded
        if: needs.DetectChanges.outputs.docs_only != 'true'
        env:
          # DetectChanges is included on purpose: if it failed, docs_only is ''
          # (!= 'true'), so we land here — and must fail rather than green-light
          # a run whose change classification never happened.
          RESULT_DETECTCHANGES: ${{ needs.DetectChanges.result }}
          RESULT_DEB: ${{ needs.Deb.result }}
          # Matrix job: 'success' only when every leg succeeded (fail-fast is
          # off, so one red release still surfaces here).
          RESULT_DEBINSTALL: ${{ needs.DebInstall.result }}
        run: |
          set -u
          rc=0
          for leg in DETECTCHANGES DEB DEBINSTALL; do
            var="RESULT_${leg}"
            result="${!var}"
            printf '%-14s %s\n' "$leg" "${result:-<missing>}"
            # Anything that is not an outright success fails the gate —
            # including 'skipped', which on the non-docs_only path can only
            # mean a leg was bypassed by a condition it should not have been.
            [ "$result" = "success" ] || rc=1
          done
          if [ "$rc" -ne 0 ]; then
            echo "::error::Linux packaging lane is not green — see the leg results above."
            exit 1
          fi
          echo "All Linux packaging legs succeeded."
